<Window x:Class="UniversalOfficeDataManager.Views.FieldManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Fields" Height="480" Width="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="FieldsList" Grid.Row="0" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding FieldType, StringFormat=' ({0})'}" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text=" *" Foreground="#DC2626"
                                   Visibility="{Binding IsRequired, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Add Field" Width="100" Margin="0,0,8,0" Click="Add_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Edit" Width="80" Margin="0,0,8,0" Click="Edit_Click"/>
            <Button Content="Delete" Width="80" Click="Delete_Click"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Close" Width="90" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Close_Click"/>
    </Grid>
</Window>
