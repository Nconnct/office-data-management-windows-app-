<UserControl x:Class="UniversalOfficeDataManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <TextBlock Text="Dashboard" FontSize="26" FontWeight="Bold" Margin="0,0,0,20"/>

            <WrapPanel>
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Workspaces" Style="{StaticResource StatLabelStyle}"/>
                        <TextBlock Text="{Binding TotalWorkspaces}" Style="{StaticResource StatValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Sheets" Style="{StaticResource StatLabelStyle}"/>
                        <TextBlock Text="{Binding TotalSheets}" Style="{StaticResource StatValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Records" Style="{StaticResource StatLabelStyle}"/>
                        <TextBlock Text="{Binding TotalRecords}" Style="{StaticResource StatValueStyle}"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <TextBlock Text="Recently Updated Sheets" FontSize="18" FontWeight="SemiBold" Margin="0,28,0,8"/>

            <ItemsControl ItemsSource="{Binding RecentSheets}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListRowStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding SheetName}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding WorkspaceName}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Text="{Binding RecordCount, StringFormat={}{0} records}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Margin="0,16,0,0" Foreground="#94A3B8" TextWrapping="Wrap"
                       Text="No sheets yet — click &quot;+ New&quot; next to Workspaces, then add a sheet, to get started."
                       Visibility="{Binding RecentSheets.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
